# Imprint

React + Bun + TanStack Start app that renders pdfcn documents with Takumi or Forme, previews them live, and stores designs in local SQLite.

```bash
bun install
bun test
bun run dev
```

Open http://localhost:3000. Agents use the HTTP contract in `AGENTS.md`.
